# K. Houfek, T. N. Rescigno, C. W. McCurdy, Phys. Rev. A 77, 012710 (2008) — Probing the nonlocal approximation to resonant collisions of electrons with diatomic molecules

**Source:** `reference/literature/houfek-2008-pra77-012710.pdf` (gitignored) · DOI 10.1103/PhysRevA.77.012710
**Pagination:** per-article, 012710-1 … 012710-14 (extractor page N = printed page 012710-N;
offset zero, checked at page 1 and page 14).

## Why this repository cares

This is "paper II", the direct sequel to Houfek/Rescigno/McCurdy 2006 (PRA 73, 032721 —
`houfek-2006-pra73-032721.md`, "paper I"), reusing paper I's exact 2-D model but adding
the **nonlocal resonance theory** (Feshbach projection-operator formalism) as a second,
more elaborate approximation to compare against the exact solution alongside the plain
local-complex-potential (LCP) reduction paper I already introduced. The repo does not
implement the nonlocal model itself (see Not used here) — what it takes is the paper's
central methodological finding: **the outcome of any resonance approximation depends on
how the "discrete state" φ_d is chosen**, and the paper enumerates and tests several
choices, including the one qscat's LCP construction (`qscat.ecs.find_resonance_pole`,
`qscat.core.lcp.local_complex_potential`) already uses implicitly. Without this source,
the repo's documented LCP failure modes — missing non-resonant elastic background,
divergent near-threshold behavior (`docs/physics/diatomic-ve-cross-sections.md`) — would
be observed facts with no explanation; this paper supplies the explanation (omitted
background T-matrix terms and an energy-independent width) and independently predicts
both, in the same F₂ model this repo runs.

## What this repository uses

| Fact | Locator | Used by |
|---|---|---|
| Sequel to paper I; same 2-D model, energy-normalized continuum states (differs from paper I's normalization) | p. 012710-1–2, Eq. (3)-(4) | context only — the repo follows paper I's own normalization, see paper I's note |
| Feshbach projector `Q = \|φ_d⟩⟨φ_d\|` defining the discrete/resonant state | p. 012710-3, Eq. (15) | conceptual basis for what `Vd(R)`/`Gamma(R)` represent physically |
| Discrete-state potential `Vd(R)` and coupling `Vdk(R)` (paper's own `Vd`, NOT qscat's `Vd`) | p. 012710-3, Eq. (20)-(21) | terminology cross-check only, see Terminology map |
| Condition that `φ_d(r;R) → φ_b(r)` (bound state) as `R→∞`, so `Vdk(R)→0` | p. 012710-7, Eq. (67) | conceptual — matches qscat's own `Vd(R) = v0(R) + s_asym` asymptotic behavior in the ECS tail |
| The "physical" discrete-state choice: `φ_d(r;R)` built from the fixed-R resonant electronic scattering wavefunction at each R (real part of the ECS pole energy), smoothly truncated | p. 012710-7, Sec. VI A | this is the same fixed-R electronic pole `qscat.ecs.find_resonance_pole`/`resonance_pole_walk` computes; qscat's LCP curve is built directly from it, without ever forming an explicit `φ_d(r;R)` |
| Finding: the "physical" choice's `Vd(R)` "almost coincides with" `Eres(R)` (paper I's/qscat's LCP real part) | p. 012710-8 | independent confirmation that qscat's `Vd(R) = v0(R)+E_res(R)` is exactly this "physical" quantity |
| Finding: the physical choice can break down (Born-Oppenheimer failure) for DA, which has no background-term correction available | p. 012710-8–9, Sec. VI A | explains, not contradicts, qscat's own documented near-threshold DA departure |
| R-independent discrete state + background terms reproduces exact cross sections for VE (elastic and inelastic) and DA in all three molecules | p. 012710-9–10, Sec. VI B, Fig. 8 | shows the missing ingredient in a bare LCP curve is precisely the background T-matrix contribution qscat's LCP does not compute |
| Plain LCP (paper I's `Eres(R)-iΓ(R)/2`, energy-independent width) gets the DA threshold behavior WRONG, unlike the nonlocal model's energy-dependent coupling | p. 012710-9 | direct explanation for the repo's own documented near-threshold LCP departure (`docs/physics/diatomic-ve-cross-sections.md`) |
| Background contributions to VE T-matrix matter for BOTH elastic and inelastic channels, largest for F₂ (broadest resonance) | p. 012710-1 (abstract), 012710-10, Sec. VI B/C | direct explanation for the repo's documented "LCP elastic omits the non-resonant background" finding |
| F₂-like model parameters (Table I) | p. 012710-6, Table I | `qscat.model.library.F2` — the actual parameter source (paper I did NOT include F₂) |
| DA cross section has no background-T-matrix analog within the nonlocal model | p. 012710-6, Sec. III C | context for why DA is the cleaner test case in both this paper and the repo |

## Equations

```
H = H0 + Vint(R,r),  H0 = TR + V0(R) + Tr + l(l+1)/2r^2         p. 012710-2, Eq. (5)-(6)
Q = |phi_d><phi_d|                                               p. 012710-3, Eq. (15)
Vd(R)      = V0(R) + <phi_d|Hel|phi_d>                           p. 012710-3, Eq. (20)
Vdk^+(R)   = <phi_d|Hel|phi_k^+>                                 p. 012710-3, Eq. (21)
lim_{R->inf} phi_d(r;R) = phi_b(r)                               p. 012710-7, Eq. (67)
[E - TR - Vd(R)] Psi_d^+(R) - Int F(E,R,R') Psi_d^+(R') dR'
    = Vdki^+(R) chi_vi(R)                                        p. 012710-5, Eq. (52)
F(E,R,R') = Int Vdk^+(R) G0^+(E,R,R') Vdk^+(R')* k dk             p. 012710-5, Eq. (53)
sigma_DA_vi(E) = 2 pi^2 (KDA/mu) / ki^2 * lim_{R->inf} |Psi_d^+(R)|^2   p. 012710-5, Eq. (54)
Gamma(E,R) = 2 pi |Vdk^+(R)|^2,  E = k^2/2                        p. 012710-8, Eq. (68)
V0(R)     = D0(e^{-2 alpha0(R-R0)} - 2 e^{-alpha0(R-R0)})         p. 012710-6, Eq. (62)
Vint(r,R) = -lambda(R) exp(-alpha(R) r^2)                        p. 012710-6, Eq. (63)
lambda(R) = lambda_inf + lambda0/(1+e^{lambda1(R-R_lambda)})     p. 012710-6, Eq. (64)-(65)
```

Atomic units throughout (hartree, bohr) — same convention as `libs/qscat/qscat/units.py`.
Eq. (62)-(66) are the identical `V0`/`Vint`/`lambda`/`alpha` forms as paper I Eq. (45)-(48);
this paper adds a third parameter set (F₂) on the same functional form.

## Parameters and numeric values

Table I, p. 012710-6 (atomic units), F₂ column (N₂/NO columns repeat paper I's Table I
values unchanged):

| Parameter | F₂ |
|---|---|
| μ | 17315.99 |
| l | 1 (p wave) |
| D0 | 0.0598 |
| α0 | 1.5161 |
| R0 | 2.6906 |
| λ∞ | 18.8490 |
| λ1 | 3.2130 |
| R_λ | 1.8320 |
| λc | 18.1450 |
| Rc | 2.5950 |
| αc | 3.0000 |

Checked against the repo:

```
grep -n "17315.99\|0.0598\|1.5161\|2.6906\|18.8490\|3.2130\|1.8320\|18.1450\|2.5950" \
  libs/qscat/qscat/model/library.py
```

`qscat.model.library.F2` (`mu=17315.99, ell=1, D0=0.05980, alpha0=1.51610, R0=2.69060,
lambda_inf=18.8490, lambda_1=3.21300, R_lambda=1.8320, lambda_c=18.1450, R_c=2.5950,
alpha_c=3.0`) matches Table I exactly (verified 2026-08-16). This supersedes paper I's
note, which correctly stated F₂ was **not** parametrized in paper I — it is parametrized
here, in paper II, and is the actual published source for `qscat.model.library.F2`
(previously attributed only to the eMoScat deck).

## Findings and limits

- **The central methodological point (why the repo cites this paper):** the "discrete
  state" `φ_d(r;R)` is not unique, and the nonlocal-model results — and, this note argues,
  the qualitative behavior of any resonance approximation built on a fixed-R electronic
  quantity — depend materially on which choice is made. Three choices are tested:
  - **(A) "Physical"/intuitive** — `φ_d` built from the fixed-R resonant electronic
    scattering wavefunction (real part of the ECS pole energy), truncated smoothly —
    p. 012710-7–9, Sec. VI A. This is the quantity qscat's `find_resonance_pole` computes
    and `local_complex_potential` uses directly as `Vd(R)`/`Gamma(R)`; the paper states
    it "almost coincides with" `Eres(R)` (p. 012710-8). Result: **can break down**
    (Born-Oppenheimer failure) at small R where `φ_d` varies rapidly with R, most
    severely for DA (no background term available to compensate) and for NO/F₂ (larger,
    more R-sensitive resonance width than N₂) — p. 012710-8–9.
  - **(B) R-independent** — `φ_d = φ_b` (the R→∞ bound state) for all R — p. 012710-9–10,
    Sec. VI B. Result: **essentially exact** for VE and DA in all three molecules,
    provided the background T-matrix terms (Eq. 37 of this paper) are included; without
    them, elastic and even some inelastic VE channels are wrong.
  - **(C) "Compact"** — a smoothly-R-varying, more localized `φ_d` tuned to minimize
    background contributions — p. 012710-10–11, Sec. VI C. Result: for F₂ (the molecule
    with the largest resonance width and thus largest background terms), certain tunings
    reproduce the exact resonant-only T-matrix (Eq. 31) well even **without** background
    terms, though the paper stresses this is model-specific, not a general recipe.
- **What this implies for qscat's plain LCP** (not the nonlocal model at all — qscat
  builds only `V_res(R) = E_res(R) - iΓ(R)/2`, paper I's/this paper's short-dashed
  curve): the paper's own comparisons show this bare curve, with **no** discrete-state
  refinement and **no** background T-matrix, systematically (i) gets DA threshold
  behavior wrong because its width is energy-independent (`Γ(R)`, not `Γ(E,R)` of
  Eq. 68) — p. 012710-9; and (ii) omits the non-resonant background contribution to
  elastic and some inelastic VE cross sections, which this paper shows is *not*
  negligible, especially for F₂ — p. 012710-1 (abstract), 012710-9–10. Both are exactly
  the qualitative departures `docs/physics/diatomic-ve-cross-sections.md` documents for
  qscat's own F₂/NO LCP runs (near-threshold σ_DA under-prediction, missing elastic
  background) — an independent, published confirmation of the failure mode, not merely
  a coincidence of this repo's implementation.
- **DA has no background-term analog** in the nonlocal formalism (Sec. III C, p. 012710-6),
  which is why DA is comparatively well-described by discrete-state choices (A) and (B)
  alike, while VE (especially elastic) is the channel most sensitive to the choice.
- The paper's own stated limits: it does not test real ab initio discrete-state
  definitions (stabilization methods, Feshbach-Fano R-matrix), only three well-defined
  parametrized choices within its own 2-D model; and it flags that hydrogen-containing
  diatomics (much smaller reduced mass than the ~1.3–1.7×10⁴ m_e systems studied here)
  would need further non-Born-Oppenheimer testing — p. 012710-12–13, Conclusion.

## Terminology map

| Paper symbol | qModeling name | Note |
|---|---|---|
| `φ_d(r;R)` | not built explicitly anywhere in qscat | qscat's LCP skips forming a discrete state and instead diagonalizes the fixed-R electronic Hamiltonian's ECS pole directly |
| `Eres(R)` (paper I's/this paper's LCP real part) | `qscat.core.lcp`'s `Vd`/`Vd(R)`, `docs/physics/lcp-resonance-levels.md`'s `E_res(R)` | **NOT** the same as this paper's own `Vd(R)` (Eq. 20) — see below |
| `Vd(R)` (this paper's Eq. 20, the discrete-state potential in the nonlocal model) | no qscat equivalent | a genuine naming collision: this paper's `Vd` and qscat's `Vd`/`E_res` denote different, only approximately coincident, quantities (p. 012710-8 states they "almost coincide" for the physical choice, not that they are identical) |
| `Γ(R)` (paper I's/this paper's LCP width) | `Gamma(R)` in `qscat.core.lcp`/`docs/physics/lcp-resonance-levels.md` | same quantity |
| `Γ(E,R)` (Eq. 68, energy-dependent nonlocal width) | not implemented in qscat | see Not used here |
| `Vdk^+(R)` | not implemented in qscat | the discrete-state–continuum coupling of the nonlocal model |
| resonant T-matrix `T_res` (Eq. 31) | `qscat.core.driven`'s exact T-matrix is NOT this — it is the full exact-2D T-matrix, with no resonant/background split | the repo never separates resonant from background contributions; it solves the full driven equation exactly |

## Not used here

- The full nonlocal resonance theory itself — the Feshbach `Q`/`P` projector
  construction (Eq. 15-19), the nonlocal energy-dependent potential `F(E,R,R')`
  (Eq. 53) and its DVR-basis evaluation scheme (Eq. 55-61), and the resonant/background
  T-matrix decomposition (Eq. 28-37) — is not implemented anywhere in qscat. The repo's
  approximation under test is the plain LCP (paper I), and its oracle is the exact 2-D
  driven-equation solution (`qscat.core.driven`) — this paper's nonlocal model sits
  between those two and is read here only for what it says about discrete-state choice
  and background-term importance, not adopted as code.
- The three specific discrete-state parametrizations of Sec. VI (Eq. 69-72,
  `λ_q(R)`, `λ_spec(R)`) — model-specific tuning functions used only to demonstrate
  sensitivity, not physical constants worth porting.
- Figs. 1-10 (potential surfaces, discrete-state wavefunction plots, cross-section
  comparison curves) are read for their qualitative conclusions only; no pixel/curve
  data was extracted.
- The Appendix's derivation that DA has no background term (Eq. A1-A7) is read for its
  conclusion only (recorded in Findings above), not transcribed as a derivation.
- N₂ and NO Table I entries here are unchanged from paper I and already covered by
  `houfek-2006-pra73-032721.md`; not repeated as a separate parity check.
